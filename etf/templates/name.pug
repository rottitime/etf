{% import 'macros.html' as macros with context %}

{% extends "base.pug" %}

block content

  form(method="POST")
    {{csrf_input}}

    fieldset(class="fieldset")
      legend
        h3 Name of evaluation (for your reference)

      {{macros.input("name", "Give a name for this evaluation")}}

      {{macros.buttons(prev_url)}}
