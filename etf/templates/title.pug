{% import 'macros.html' as macros with context %}

{% extends "base.pug" %}

block content

  form(method="POST")
    {{csrf_input}}

    fieldset(class="fieldset")
      legend
        h3 Title of evaluation

      {{macros.input("title", "Give a title for this evaluation")}}

      {{macros.buttons(prev_url)}}
