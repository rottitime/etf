{% import 'macros.html' as macros with context %}

{% extends "base.pug" %}

block content

  form(method="POST")
    {{csrf_input}}

    fieldset(class="fieldset")
      legend
        h3 Participant recruitment approach

      {{macros.textarea("target_population", "Target population including location(s)")}}

      {{macros.textarea("eligibility_criteria", "Eligibility criteria")}}
          
      {{macros.textarea("process_for_recruitment", "Process for recruitment")}}

      {{macros.textarea("target_sample_size", "Target sample size")}}

      {{macros.textarea("intended_recruitment_schedule", "Intended recruitment schedule including date of first recruitment")}}

      {{macros.input("date_of_first_recruitment", "Evaluation end date", type="date")}}

      {{macros.buttons(prev_url)}}
