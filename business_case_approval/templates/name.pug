{% import 'macros.html' as macros with context %}

{% extends "base.pug" %}

block content

  form(method="POST")
    {{csrf_input}}

    fieldset(class="fieldset")
      legend
        h3 Name of application (for your reference)

      {{macros.input("name", "Give a name for this application")}}

      {{macros.buttons(prev_url)}}
